Robot simulation.

This package provides a distribuited simulation. To use it just type:

make; 

and then;

./control/control
and in other window..
./simulation/simulation

It is also possible to simulate it in two computers. In order to do it, just change the 
ip address inside the file rtnetAPI.c, and recompile it;


TODO:

1) Display thread in control process;
2) Parameters adjustment(nem process sharing data with control process);
3) Calculate robot data. It is already done, but it is necessary to put it to run.
4) Calculate other data;

